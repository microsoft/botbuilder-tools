{
    "$schema": "../../src/cogSchema.schema",
    "title": "Simple Prompt",
    "description": "Example simple prompt.",
    "$role": "unionType(Microsoft.IPrompt)",
    "properties": {
        "prompt": {
            "$role": "lg",
        }
    }
}